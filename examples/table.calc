# Таблица квадратов и кубов — многострочный цикл с print.
# Запуск: calc --file examples/table.calc
fn sq(n) = n * n
fn cube(n) = n * n * n
i = 1
while (i <= 5) {
    print(i, "квадрат:", sq(i), "куб:", cube(i))
    i = i + 1
}
